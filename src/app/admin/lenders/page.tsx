import { LenderOnboarding } from '@/components/admin/LenderOnboarding';

export default function AdminLendersPage() {
  return (
    <div className="min-h-screen bg-gray-50">
      <LenderOnboarding />
    </div>
  );
}